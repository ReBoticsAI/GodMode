# Cloudflare Pages (and similar CI) auto-runs `npm clean-install` before the
# build command. Skip lifecycle scripts so native addons like node-pty (bridge)
# do not fail the marketing web build. Local bridge: `npm rebuild node-pty` if needed.
ignore-scripts=true
