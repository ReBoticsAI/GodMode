/**
 * Signed-in home is the Graph (`ChatGraphCanvas` in AppShell).
 * This route exists so `/home` stays a valid destination after auth;
 * it intentionally renders nothing.
 */
export default function Home() {
  return null;
}
