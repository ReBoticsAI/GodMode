/**
 * Shared agent loop constants for Cursor-parity iteration budgets across backends.
 */

export const DEFAULT_AGENT_ITERATIONS = 32;
export const PROVIDER_AGENT_ITERATIONS = 32;

export { DEFAULT_MAX_ITERATIONS, budgetToolResult, TOOL_OUTPUT_MAX_CHARS } from "./ai-agent.js";
